# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Zahnarzt Dr. med. dent. Sandra Daller
title_separator: "|"
name: "Dr. med. dent. Sandra Daller"
email: praxis@daller.org
telefon: "06452 / 21013"
locale: "de-AT"
description: >-
  Zahnarzt Dr. med. dent. Sandra Daller in Altenmarkt im Pongau / Salzburg. Eben, Flachau, Radstadt
logo: "/assets/images/icon.svg"
atom_feed:
  hide: true
search: true

baseurl: ""
url: "https://zahnarzt.daller.org"
domain: "zahnarzt.daller.org"

# Build settings
markdown: kramdown
remote_theme: "mmistakes/minimal-mistakes@4.24.0"
minimal_mistakes_skin: "dirt"
plugins:
  - jekyll-feed
  - jekyll-include-cache
  - jekyll-sitemap

include: ["_pages"]

defaults:
  # _pages
  - scope:
      path: ""
      type: pages
    values:
      layout: single
  # _posts
  - scope:
      path: ""
      type: posts
    values:
      layout: single
      share: true
      related: false

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
